# ─────────────────────────────────────────────────────────────
# Universal junk / VCS / OS
# ─────────────────────────────────────────────────────────────
.git
.gitmodules
.gitignore
.gitattributes
.DS_Store
Thumbs.db
*.swp
*.swo
*.orig

# Logs & reports
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log
coverage
.nyc_output

# Editor / tooling
.vscode
.idea
*.iml

# Transpiled / cache directories (any package)
dist
build
tmp
temp
.cache
.parcel-cache

# Monorepo tool caches (if used)
.turbo
.turbocache
.nx
.pnp.cjs
.pnp.loader.mjs
.pnp.data.json
.pnp.js

# ─────────────────────────────────────────────────────────────
# Web app (Next.js)
# ─────────────────────────────────────────────────────────────
.next
.next/cache
out
.vercel

# (If you export Storybook or other static sites)
storybook-static

# ─────────────────────────────────────────────────────────────
# Worker (Node/TypeScript)
# ─────────────────────────────────────────────────────────────
# Ignore *local* build outputs; Docker will compile inside the image.
# (Leave source in repo so multi-stage can build it.)
dist/
*.tsbuildinfo

# ─────────────────────────────────────────────────────────────
# Package managers (keep lockfiles!)
# ─────────────────────────────────────────────────────────────
# Ignore local modules; install fresh inside Docker.
node_modules
**/node_modules

# Keep your lockfiles for reproducible builds.
# DO NOT ignore these:
!package-lock.json
!yarn.lock
!pnpm-lock.yaml

# ─────────────────────────────────────────────────────────────
# Env & secrets (handled via GSM / CI/CD env vars)
# ─────────────────────────────────────────────────────────────
.env
.env.*
!.env.example
!.env.sample
!.env.schema

# ─────────────────────────────────────────────────────────────
# Test / e2e artifacts (optional)
# ─────────────────────────────────────────────────────────────
cypress/videos
cypress/screenshots
playwright-report
test-results

# ─────────────────────────────────────────────────────────────
# Docker local stuff
# ─────────────────────────────────────────────────────────────
docker-compose*.yml
Dockerfile.local
