// app/about/page.tsx
import AboutContent from '@/components/AboutContent';

export default function AboutPage() {
  // The layout's <main> tag will wrap this component.
  // No special classes are needed here.
  return <AboutContent />;
}